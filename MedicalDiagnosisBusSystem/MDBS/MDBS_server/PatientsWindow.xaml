<Window x:Class="MDBS_server.PatientsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MDBS_server"
        mc:Ignorable="d"
        Title="Пациенты" Height="650" Width="1100">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top" MaxHeight="32">
            <Button Click="NewPatient">Новый пациент</Button>
            <Separator></Separator>
            <Button Click="EditPatient">Редактировать</Button>
            <Separator></Separator>
            <Button Click="DeletePatient">Удалить пациента</Button>
        </ToolBar>
        <DataGrid Name="PatientGrid" IsReadOnly="True" GridLinesVisibility="Horizontal" Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" AutoGenerateColumns="True" AutoGeneratedColumns="PatientGridColumnsGenerated">
            <DataGrid.Resources>
                <Style x:Key="wordWrapStyle" TargetType="{x:Type TextBlock}">
                    <Setter Property="TextWrapping" Value="Wrap"/>
                    <Setter Property="MaxWidth" Value="200"/>
                </Style>
            </DataGrid.Resources>
        </DataGrid>
    </DockPanel>
</Window>